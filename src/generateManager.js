function generateManager(managerJSON) {
    console.log('generating manager');
    console.log(JSON.parse(managerJSON));
    return `
is this the correct way?
    `
}

module.exports = generateManager;